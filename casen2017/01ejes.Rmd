---
title: "Lo mostrado y lo implicado"
output:
  tufte::tufte_html: default
  tufte::tufte_handout: default
author: "Thomas Brochhagen"
date: "`r Sys.Date()`"
---
```{r setup, include=FALSE}
library(tufte)
# invalidate cache when the tufte version changes
knitr::opts_chunk$set(tidy = FALSE, cache.extra = packageVersion('tufte'))
options(htmltools.dir.version = FALSE)
```

```{r, echo=FALSE}
library("ggplot2"); theme_set(theme_light())
d1 <- data.frame("percent" = c(30.7, 22.8,27.3),
                    "year" = c("2010", "2013", "2016"))
myred <- 'red'
myblue <- 'blue'
myalpha <- 0.75
```


# TO DO
* Recortar Pi??era con grafico y poner como marginnote
* Texto: Preambulo, explicaci??n, proporcionalidad, shiny ejemplo 1A (Pi??i), ejemplo 1B (CASE), ejemplo 2 (CASE)

# Una es ninguna; m??s son un patr??n

```{r fig-margin, fig.margin = TRUE, fig.cap = "Arriba: Barras con escala com??n. Abajo: Barras rid??culas", fig.width=3.5, fig.height=3.5, cache=TRUE, message=FALSE, echo=FALSE}
ggplot(d1, aes(x = year, y = percent, fill = c('na','ba','na'))) +
            geom_bar(stat="identity", position = "dodge", alpha = myalpha) +
            scale_fill_manual(values=c(myblue, myred)) +
            theme(legend.position="none") + # remove legend (that comes from dummy categories)
            geom_text(aes(label=percent), vjust=3.6, color="white", size=8) +
            theme(axis.title.x=element_blank(),
                  axis.title.y=element_blank(),
                  axis.text.x = element_text(size=14),
                  axis.text.y = element_text(size=14))
```

Una de las *curiosidades* que dej?? el debate presidencial de Noviembre fueron los gr??ficos empleados por Sebasti??n Pi??era. No solo por traer gr??ficos impresos a un debate, si no porque hab??a o ignorancia o una intenci??n de manipulaci??n en su dise??o. Las barras de su gr??fico^[![](data/small-pin.png) Fuente: CNN] contrastando delincuencia del 2010, 2013 y 2016 no usan la misma escala: La barra representando la criminalidad del 2013, cuando Pi??era fue presidente por primera vez, es mucho m??s baja de lo que deber??a ser. O las otras dos demasiado altas, es lo mismo. El punto es que visualmente esta manipulaci??n sugiere que con Pi??era hay (notablemente) menos criminalidad al incrementar, visualmente, la diferencia visual con las otras dos barras. La presentaci??n de la encuesta Casen 2017^[La encuesta [Casen](http://observatorio.ministeriodesarrollosocial.gob.cl) es una de las herramientas principales que caracterizan desarrollos socio-econ??micos. Estudios y decisiones pol??ticas se basan en ella. Es mandatada por el Ministerio de Desarrollo Social de Chile.], publicada la semana pasada, muestra que nada se quiere aprender; o bien, lo mucho que se quiere manipular. 

